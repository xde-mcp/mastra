const meta = {
  workflow: "Workflow",
  "step-class": "Step",
  "step-options": "StepOptions",
  "step-condition": "StepCondition",
  "step-function": ".step()",
  after: ".after()",
  then: ".then()",
  until: ".until()",
  while: ".while()",
  if: ".if()",
  else: ".else()",
  createRun: ".createRun()",
  start: ".start()",
  execute: ".execute()",
  suspend: ".suspend()",
  resume: ".resume()",
  commit: ".commit()",
  watch: ".watch()",
};

export default meta;
