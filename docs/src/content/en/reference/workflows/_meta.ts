const meta = {
  workflow: "Workflow",
  step: "createStep()",
  then: "then()",
  parallel: "parallel()",
  branch: "branch()",
  dowhile: "dowhile()",
  dountil: "dountil()",
  foreach: "foreach()",
  map: "map()",
  commit: "commit()",
  "create-run": "createRun()",
  snapshots: "Snapshots",
  watch: "watch()",
  stream: "stream()",
  execute: "execute()",
  resume: "resume()",
  start: "start()",
};

export default meta;
