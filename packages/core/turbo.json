{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "./**/*.d.ts"],
      "inputs": ["$TURBO_DEFAULT$", "./tsup.config.ts"]
    }
  }
}
