{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "prebuild"],
      "outputs": ["dist/**", "./**/*.d.ts"],
      "inputs": ["$TURBO_DEFAULT$", "./tsup.config.ts"]
    }
  }
}
